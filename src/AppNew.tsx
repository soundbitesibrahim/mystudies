import React, { useMemo, useState, type ReactNode } from 'react';

const subjects = ['Physics 0625','Chemistry 0620','Mathematics','English 0500','Computer Science 0478'];

type Question = { q:string; a:string; hint:string; explain:string };
type Topic = { topic:string; concept:string; questions:Question[] };

const topicBank: Record<string,Topic> = {
  'Physics 0625': { topic:'Forces & motion', concept:'Resultant force is the overall force after adding forces with direction taken into account.', questions:[
    {q:'A car has 900 N driving force and 650 N resistive force. What is the resultant force?',a:'250 N forward',hint:'Subtract the opposing force.',explain:'The forces act in opposite directions, so 900 − 650 = 250 N forward.'},
    {q:'What happens to acceleration when resultant force increases while mass stays constant?',a:'It increases',hint:'Use F = ma.',explain:'With the same mass, a bigger resultant force gives a bigger acceleration.'},
    {q:'A 4 kg object accelerates at 3 m/s². Find the resultant force.',a:'12 N',hint:'Multiply mass by acceleration.',explain:'F = ma = 4 × 3 = 12 N.'},
    {q:'Two forces of 18 N right and 7 N left act on an object. What is the resultant?',a:'11 N right',hint:'Opposite directions subtract.',explain:'18 − 7 = 11 N, and the larger force is to the right.'},
    {q:'An object moves at constant velocity. What can you say about the resultant force?',a:'0 N',hint:'Think about acceleration.',explain:'Constant velocity means zero acceleration, so F = ma gives zero resultant force.'}
  ]},
  'Chemistry 0620': { topic:'Atomic structure', concept:'Atoms contain protons and neutrons in the nucleus, with electrons arranged around it.', questions:[
    {q:'Which particle has a positive charge?',a:'Proton',hint:'It is found in the nucleus.',explain:'A proton has relative charge +1.'},
    {q:'An atom has 11 protons and 10 electrons. What is its overall charge?',a:'+1',hint:'Compare positive and negative charges.',explain:'There is one more positive charge than negative charge, so the ion is +1.'},
    {q:'What is the relative charge of an electron?',a:'−1',hint:'Electrons are negatively charged.',explain:'Each electron has relative charge −1.'},
    {q:'Why is an atom normally neutral?',a:'It has equal numbers of protons and electrons',hint:'Positive and negative charges cancel.',explain:'Equal positive proton charge and negative electron charge give a net charge of zero.'},
    {q:'An atom has proton number 17. How many electrons does the neutral atom have?',a:'17',hint:'A neutral atom has equal positive and negative charge.',explain:'Neutral means electrons = protons, so it has 17 electrons.'}
  ]},
  Mathematics: { topic:'Algebra & equations', concept:'Solve equations by doing the same operation to both sides and keeping the variable isolated.', questions:[
    {q:'Solve: 3x + 5 = 20',a:'x = 5',hint:'Subtract 5, then divide by 3.',explain:'3x = 15, so x = 5.'},
    {q:'Solve: 7x − 14 = 28',a:'x = 6',hint:'Add 14, then divide by 7.',explain:'7x = 42, so x = 6.'},
    {q:'Expand: 4(x + 3)',a:'4x + 12',hint:'Multiply 4 by every term inside.',explain:'4 × x + 4 × 3 = 4x + 12.'},
    {q:'Factorise: 6x + 18',a:'6(x + 3)',hint:'Take out the highest common factor.',explain:'The highest common factor is 6.'},
    {q:'Solve: 2(x + 4) = 18',a:'x = 5',hint:'Expand or divide first.',explain:'2x + 8 = 18, so 2x = 10 and x = 5.'}
  ]},
  'English 0500': { topic:'Directed writing', concept:'Strong directed writing stays focused on purpose, audience and form while using precise vocabulary and controlled paragraphs.', questions:[
    {q:'What should you decide first in a directed-writing task?',a:'Purpose, audience and form',hint:'Think: why, who, and what type of text.',explain:'Those three choices control your tone, ideas and structure.'},
    {q:'Which is stronger for formal writing: “really bad” or “deeply concerning”?',a:'Deeply concerning',hint:'Choose precise vocabulary.',explain:'“Deeply concerning” is more precise and controlled for a formal audience.'},
    {q:'Why should each paragraph have a clear purpose?',a:'To keep ideas organised and easy to follow',hint:'Think about structure and coherence.',explain:'Clear paragraph roles make your argument easier for the reader to follow.'},
    {q:'What should your tone sound like when writing a formal school report?',a:'Controlled, clear and appropriately formal',hint:'Avoid slang and overly casual language.',explain:'The tone should fit the audience and purpose rather than sounding like casual speech.'},
    {q:'Which opening best suits a formal school report: “You guys will not believe this!” or “The event had several significant outcomes.”?',a:'The event had several significant outcomes.',hint:'Match tone to audience and form.',explain:'A report needs controlled, informative language rather than an excited conversational opening.'}
  ]},
  'Computer Science 0478': { topic:'Pseudocode — selection', concept:'Selection lets a program choose between different paths using a condition.', questions:[
    {q:'Which structure is normally used when a program must choose between two paths?',a:'IF ... THEN ... ELSE',hint:'It checks a condition.',explain:'The IF condition chooses one branch when true and the ELSE branch otherwise.'},
    {q:'What is the result of IF score >= 50 THEN PASS ELSE FAIL when score = 42?',a:'FAIL',hint:'42 is not at least 50.',explain:'The condition score >= 50 is false, so the ELSE branch runs.'},
    {q:'What does a condition evaluate to?',a:'TRUE or FALSE',hint:'A decision needs a yes/no outcome.',explain:'A Boolean condition gives either TRUE or FALSE.'},
    {q:'Write a simple selection to output “Adult” when age is 18 or more, otherwise “Under 18”.',a:'IF age >= 18 THEN OUTPUT "Adult" ELSE OUTPUT "Under 18"',hint:'Use a comparison and two branches.',explain:'The condition checks whether age is at least 18, then selects the matching output.'},
    {q:'What is one advantage of using selection in an algorithm?',a:'It lets the algorithm make decisions based on conditions',hint:'Think about different possible paths.',explain:'Selection allows an algorithm to behave differently for different inputs.'}
  ]}
};

const nav = ['Home','Study','My Subjects','Planner','Mistakes','Progress','AI Coach','Settings'];
function Signal({children}:{children:ReactNode}){return <span className="signal">{children}</span>}

export default function AppNew(){
  const [active,setActive]=useState('Home');
  const [subject,setSubject]=useState('Physics 0625');
  const [dark,setDark]=useState(false);
  const [coachOpen,setCoachOpen]=useState(false);
  const [sessionOpen,setSessionOpen]=useState(false);
  const [qIndex,setQIndex]=useState(0);
  const [answer,setAnswer]=useState('');
  const [feedback,setFeedback]=useState('');
  const [mastery,setMastery]=useState<Record<string,number>>({'Physics 0625':72,'Chemistry 0620':48,'Mathematics':84,'English 0500':53,'Computer Science 0478':61});
  const [xp,setXp]=useState(1240);
  const [streak]=useState(9);
  const [history,setHistory]=useState<string[]>([]);
  const bank=topicBank[subject];
  const currentQuestion=bank.questions[qIndex % bank.questions.length];
  const status=useMemo(()=>mastery[subject]>=90?'Mastered':mastery[subject]>=75?'Strong':mastery[subject]>=60?'Building':mastery[subject]>=40?'Developing':'Starting',[mastery,subject]);
  function openStudy(s=subject){setSubject(s);setActive('Study');setSessionOpen(true);setFeedback('');setAnswer('');setQIndex(0)}
  function nextQuestion(){setQIndex(v=>v+1);setFeedback('');setAnswer('')}
  function submit(){
    if(!answer.trim())return;
    const clean=answer.trim().toLowerCase().replace(/[.,]/g,'');
    const target=currentQuestion.a.toLowerCase().replace(/[.,]/g,'');
    const correct=clean===target || target.includes(clean) || clean.includes(target);
    setMastery(m=>({...m,[subject]:Math.max(0,Math.min(100,m[subject]+(correct?4:-2)))}));
    setXp(x=>x+(correct?35:10));
    setHistory(h=>[`${subject} · ${bank.topic}: ${correct?'Correct':'Needs review'}`,...h].slice(0,10));
    setFeedback(correct?`Correct. ${currentQuestion.explain}`:`Not quite. ${currentQuestion.explain}`)
  }
  const page=active==='Home'?<div className="page homePage">
    <section className="hero"><div className="eyebrow"><Signal>● LIVE COACH</Signal><span>Built around your syllabus</span></div><h1>Know exactly what to study.</h1><p>One clear next move, based on your subjects, mastery, mistakes and deadlines.</p><button className="primary big" onClick={()=>openStudy(subject)}>What should I study? <span>→</span></button><div className="heroMeta"><span><b>{xp.toLocaleString()}</b> XP</span><span><b>{streak}</b> day streak</span><span><b>{Object.keys(topicBank).length}</b> subjects</span></div></section>
    <section className="focusRow"><div className="focusMain"><div className="sectionTop"><div><span className="kicker">NEXT BEST ACTION</span><h2>Strengthen {bank.topic}</h2></div><span className="masteryPill">{mastery[subject]}% · {status}</span></div><p>{bank.concept}</p><div className="progressLine"><span style={{width:`${mastery[subject]}%`}}/></div><div className="focusFooter"><span>Chosen from your current mastery.</span><button className="textButton" onClick={()=>openStudy(subject)}>Start session →</button></div></div><aside className="attention"><span className="kicker">NEEDS ATTENTION</span>{subjects.filter(s=>mastery[s]<65).slice(0,3).map(s=><button key={s} className="attentionItem" onClick={()=>openStudy(s)}><span>{s}</span><b>{mastery[s]}%</b></button>)}</aside></section>
    <section className="subjectsStrip"><div className="sectionTop"><div><span className="kicker">YOUR SUBJECTS</span><h2>Pick a subject</h2></div><button className="textButton" onClick={()=>setActive('My Subjects')}>View all →</button></div><div className="subjectRail">{subjects.map((s,i)=><button className={`subjectTile ${s===subject?'selected':''}`} key={s} onClick={()=>openStudy(s)}><small>{String(i+1).padStart(2,'0')}</small><strong>{s}</strong><span>{mastery[s]}% · {mastery[s]>=75?'Strong':'Focus'}</span></button>)}</div></section>
  </div>:active==='Study'?<div className="page studyPage"><div className="studyHeader"><div><span className="kicker">STUDY MODE</span><h1>{subject}</h1><p>{bank.topic} · {mastery[subject]}% mastery</p></div><button className="ghost" onClick={()=>setSessionOpen(true)}>Start session</button></div><div className="lessonGrid"><div className="lessonLead"><span className="lessonNumber">01</span><h2>{bank.topic}</h2><p>{bank.concept}</p><button className="primary" onClick={()=>setSessionOpen(true)}>Start adaptive session →</button></div><div className="lessonFacts"><div><span>MASTERY</span><b>{mastery[subject]}%</b></div><div><span>LEVEL</span><b>{status}</b></div><div><span>VARIATIONS</span><b>{bank.questions.length}</b></div></div></div><div className="variationList"><span className="kicker">RECENT ACTIVITY</span>{history.length?history.map((x,i)=><div className="activityLine" key={i}><span>{x}</span><b>→</b></div>):<div className="emptyLine">No attempts yet for this session.</div>}</div></div>:active==='My Subjects'?<div className="page"><div className="pageTitle"><span className="kicker">SYLLABUS</span><h1>My subjects</h1><p>Every subject has its own topic bank and study flow.</p></div><div className="subjectDirectory">{subjects.map((s,i)=><button key={s} className="directoryItem" onClick={()=>openStudy(s)}><span className="dirNum">{String(i+1).padStart(2,'0')}</span><div><h3>{s}</h3><p>{topicBank[s].topic}</p></div><strong>{mastery[s]}%</strong><span>Open →</span></button>)}</div></div>:active==='Planner'?<div className="page"><div className="pageTitle"><span className="kicker">PLANNER</span><h1>Today, without the guesswork.</h1><p>Study around schoolwork, not against it.</p></div><div className="timeline"><div className="timelineItem done"><span>09:00</span><div><b>Mathematics</b><p>Algebra mixed questions · 45 min</p></div><em>Done</em></div><div className="timelineItem current"><span>16:30</span><div><b>{subject}</b><p>{bank.topic} adaptive session · 35 min</p></div><em>Next</em></div><div className="timelineItem"><span>17:15</span><div><b>Computer Science</b><p>Pseudocode recall · 30 min</p></div><em>Later</em></div></div></div>:active==='Mistakes'?<div className="page"><div className="pageTitle"><span className="kicker">MISTAKE BANK</span><h1>Turn mistakes into marks.</h1><p>Wrong answers become targeted review.</p></div><div className="mistakePanel">{history.length?history.map((x,i)=><div className="mistakeRow" key={i}><span className="mistakeDot">!</span><div><b>{x}</b><p>Review this topic and try another variation.</p></div><span>Review ↗</span></div>):<div className="emptyState">No mistakes saved yet. Answer a question and weak points will show here.</div>}</div></div>:active==='Progress'?<div className="page"><div className="pageTitle"><span className="kicker">PROGRESS</span><h1>Your trajectory.</h1><p>See where your mastery is moving.</p></div><div className="progressTable">{subjects.map(s=><div className="progressRow" key={s}><div><b>{s}</b><span>{topicBank[s].topic}</span></div><div className="miniTrack"><span style={{width:`${mastery[s]}%`}}/></div><strong>{mastery[s]}%</strong></div>)}</div></div>:active==='AI Coach'?<div className="page coachPage"><div className="pageTitle"><span className="kicker">AI COACH</span><h1>Ask. Decide. Study.</h1><p>No generic “study harder” answers — get a concrete next action.</p></div><div className="coachPrompt"><button onClick={()=>openStudy()}>“What should I study right now?”</button><button onClick={()=>openStudy('Chemistry 0620')}>“Help my weakest subject.”</button><button onClick={()=>openStudy('English 0500')}>“Give me a different English question.”</button></div></div>:<div className="page"><div className="pageTitle"><span className="kicker">SETTINGS</span><h1>Make MyStudies yours.</h1><p>Theme and study preferences.</p></div><div className="settingsList"><button onClick={()=>setDark(v=>!v)}><span>Appearance</span><b>{dark?'Dark':'Light'}</b></button><button><span>Daily target</span><b>3h 30m</b></button><button><span>Goal</span><b>A / A*</b></button></div></div>;
  return <div className={dark?'app dark':'app'}><aside className="sidebar"><div className="brand"><span className="brandMark">M</span><span>MyStudies</span></div><nav>{nav.map(item=><button key={item} className={active===item?'navItem active':'navItem'} onClick={()=>setActive(item)}><span className="navGlyph">{item==='Home'?'⌂':item==='Study'?'◉':item==='My Subjects'?'◫':item==='Planner'?'◷':item==='Mistakes'?'!':item==='Progress'?'↗':item==='AI Coach'?'✦':'⚙'}</span>{item}</button>)}</nav><div className="sidebarBottom"><button className="coachMini" onClick={()=>setCoachOpen(v=>!v)}><span>✦</span><div><b>AI Coach</b><small>Ready when you are</small></div></button><div className="profile"><span className="avatar">I</span><div><b>Ibrahim</b><small>Y9 · A/A*</small></div></div></div></aside><main className="main"><header className="topbar"><div className="crumb">{active}<span>/</span>{subject}</div><div className="topActions"><button className="searchBtn" onClick={()=>setCoachOpen(true)}>Search <kbd>Ctrl K</kbd></button><button className="iconBtn" onClick={()=>setDark(v=>!v)}>{dark?'☀':'◐'}</button><button className="coachTop" onClick={()=>setCoachOpen(true)}>Ask coach <span>↗</span></button></div></header>{page}</main>
    {coachOpen&&<div className="coachDrawer"><div className="drawerHead"><div><span className="kicker">AI COACH</span><h3>What do you need?</h3></div><button onClick={()=>setCoachOpen(false)}>×</button></div><p>I can recommend a subject, help a weak area, explain a topic or give another question.</p><div className="coachChoices"><button onClick={()=>{setCoachOpen(false);openStudy(subject)}}>What should I study?</button><button onClick={()=>{setCoachOpen(false);openStudy('Chemistry 0620')}}>Help my weakest subject</button><button onClick={()=>{setCoachOpen(false);openStudy()}}>Give me a fresh question</button></div></div>}
    {sessionOpen&&<div className="modalShade"><div className="sessionModal"><div className="sessionTop"><div><span className="kicker">ADAPTIVE SESSION · {subject}</span><h2>{bank.topic}</h2></div><button onClick={()=>setSessionOpen(false)}>×</button></div><div className="sessionSteps"><span className="on">Learn</span><span className="on">Recall</span><span className="on">Question</span><span className="on">Mark</span><span className={feedback?'on':''}>Review</span><span>Mini-test</span></div><div className="questionBox"><span className="questionTag">QUESTION {qIndex+1} · VARIATION {(qIndex%bank.questions.length)+1}/{bank.questions.length}</span><h3>{currentQuestion.q}</h3><textarea value={answer} onChange={e=>setAnswer(e.target.value)} placeholder="Type your answer..."/><div className="questionActions"><button className="hint" onClick={()=>setFeedback(`Hint: ${currentQuestion.hint}`)}>Need a hint</button><button className="primary" onClick={submit}>Check answer</button></div>{feedback&&<div className={feedback.startsWith('Correct')?'feedback good':'feedback'}>{feedback}</div>}</div>{feedback&&<div className="explainBox"><span className="kicker">COACH NOTE</span><p>{currentQuestion.explain}</p><button className="textButton" onClick={nextQuestion}>Next question →</button></div>}</div></div>}
  </div>
}
